package com.wakfoverlay.domain.fight.model;

import java.util.Optional;

public record StatusEffect(
        String name
        // TODO: add mapping between some status effects (like Toxine and Tetratoxine)
) {
}
